#include<stdio.h>
int main()
{
int a,b;
printf("enter a first no:");
scanf("%d",&a);
printf("enter an second no:");
scanf("%d",&b);
if(a>b)
{
printf("a is the greatest %d",a);
}
else
{
printf("b is the greatest %d",b);
}
return 0;
}
